node_modules/
.DS_Store
axe-results.json
.verify-backup/

# Another session's git worktrees live here; they are not part of the site.
.claude/worktrees/

# Local tool permissions — one person's machine, not a repo policy.
.claude/settings.local.json

# The implementation brief is Thomas's working document, not a repo artefact.
nativity_website_claude_implementation_brief.md

# Sandbox credentials override; the defaults live in .env.example.
tools/evo-sandbox/.env
